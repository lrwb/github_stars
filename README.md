read github api, store fields in db, display table on web app

Create Installation Environment
`$ python3.6 -m venv env3`
`$ source env3/bin/activate`
`$ pip install -r requirements.txt`
`$ deactivate`
